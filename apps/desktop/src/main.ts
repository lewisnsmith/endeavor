import { endeavorVersion } from "@endeavor/shared-types";

function bootstrapDesktopApp(): void {
  console.log(`[desktop] Endeavor initializing (contracts ${endeavorVersion})`);
}

bootstrapDesktopApp();

