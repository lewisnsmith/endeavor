/**
 * CLI handlers — placeholder for Sprint 2 (E5).
 * Will implement command routing from @endeavor/cli.
 */

export function handleCliCommand(_command: string, _args: string[]): void {
  // Not implemented yet
}
